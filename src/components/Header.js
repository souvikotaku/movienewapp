import React from "react";
import "../styles/Header.css"; // Optional, if you have specific styles

const Header = () => (
  <header className="header">
    <h1>Movie Insights Dashboard</h1>
  </header>
);

export default Header;
